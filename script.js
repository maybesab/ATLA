// window.addEventListener('scroll', () => {
//     const hero = document.querySelector('.hero-parallax');
//     let scrollY = window.scrollY;
//     hero.style.transform = `scale(${1.15 - scrollY * 0.0006})`;
//   });
  